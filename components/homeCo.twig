<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Mecado</title>
    <link href="style/Mecado.css" rel="stylesheet">
    <link href="style/scss/main.css" rel="stylesheet">

</head>

<body>
<header>
    <div class="header">
        <div>
            <h1>Mecado</h1>
            <h6>Bonjour, {{ creator.name }} {{ creator.firstName }} !</h6>
        </div>

        <div  class="text-right btnLogNav ">

            <a href="/disconnect" class="btn-link btn-flat">Se deconnecter</a>

        </div>
    </div>

</header>
<nav>
    <ul class="navbar bcg">
        <li><a href="/creatList">Créer liste</a></li>
        <li><a href="#">partager liste</a></li>
        <li><a href="#">AIDE</a></li>
    </ul>
</nav>

<h2>Voici toutes vos listes</h2>

<div>
    {%  if listsArray |length > 0 %}
        <div class="row bcg">
            {% for lists in listsArray %}
                <div class="col col-span-6 bcg-col">
                    <h1 class = "titleLists">{{ lists.title}}</h1>
                    <hr>
                    <h4  class = "titleLists">Description</h4> <p>{{ lists.description }}</p>
                    <a href="/homeCo/{{lists.id}}/item" type="button" class="btn-link textRight">Afficher le contenu de la liste</a>
                    <div class="text-left">
                        <h4 class = "titleLists">Date de validité</h4> <p>{{ lists.validityDate }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>

</div>

    {% endif %}

</body>

</html>
